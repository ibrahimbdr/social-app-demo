import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'social-app-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
